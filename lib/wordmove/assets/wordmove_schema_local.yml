type: map
mapping:
  vhost:
    pattern: /^https?:\/\//
  wordpress_path:
    required: true
  database:
    type: map
    required: true
    mapping:
      name:
        required: true
      user:
        required: true
      password:
        required: true
      host:
        required: true
  paths:
    type: map
    mapping:
      wp_content:
      wp_config:
      uploads:
      plugins:
      mu_plugins:
      themes:
      languages:
