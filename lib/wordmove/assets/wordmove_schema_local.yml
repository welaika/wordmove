type: map
mapping:
  vhost:
    pattern: /^http:\/\//
  wordpress_path:
  database:
    type: map
    required: true
    mapping:
      name:
        required: true
      user:
        required: true
      password:
        required: true
      host:
        required: true
      mysqldump_options:
      port:
      charset:
