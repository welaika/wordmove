language: ruby

rvm:
  - 2.6.5

cache: bundler

before_install:
  - gem update --system
  - gem install bundler

install:
  - bundle install --jobs=3 --retry=3

deploy:
  provider: script
  script: bash deploy/deploy.sh
  on:
    tags: true
    branch: master
    ruby: '2.6.5'
